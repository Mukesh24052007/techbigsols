# Dependencies — reinstalled inside the container
node_modules

# Next.js build output — rebuilt inside the container
.next

# Local environment files — secrets must be injected at runtime via
# ECS task definition env vars, AWS Parameter Store, or Secrets Manager
.env
.env.local
.env.development
.env.production

# Version control
.git
.gitignore

# Kiro / editor config
.kiro

# Misc
README.md
AGENTS.md
CLAUDE.md
*.md
Dockerfile
.dockerignore
